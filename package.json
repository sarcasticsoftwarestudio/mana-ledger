{
  "name": "secret-lair-tracker",
  "version": "1.7.10",
  "description": "MTG Secret Lair & collection tracker — desktop edition",
  "main": "src/main/main.js",
  "scripts": {
    "start": "npm run build:renderer && electron .",
    "dev": "npm run build:renderer && electron . --dev",
    "build:renderer": "vite build",
    "build:renderer:watch": "vite build --watch",
    "build": "npm run build:renderer && electron-builder",
    "build:steam": "npm run build:renderer && electron-builder --win dir --config.extraMetadata.slChannel=steam --publish never",
    "steam:ship": "node scripts/steam-upload.js",
    "release": "npm run build:renderer && electron-builder --win --publish always",
    "release:tag": "node scripts/release.js",
    "rebuild": "electron-rebuild -f -w better-sqlite3",
    "test": "vitest run",
    "test:smoke": "node scripts/smoke-bulkdata.js && node scripts/smoke-decks.js && node scripts/smoke-decks-render.js && node scripts/smoke-droppnl.js && node scripts/smoke-help-center.js && node scripts/smoke-import.js && node scripts/smoke-insights.js && node scripts/smoke-portfolio.js && node scripts/smoke-realized.js && node scripts/smoke-search-containment.js && node scripts/smoke-settings.js && node scripts/smoke-sldrop-search.js && node scripts/smoke-slexplorer.js && node scripts/smoke-slindex.js && node scripts/smoke-sl-price-seed.js && node scripts/smoke-wantlist.js",
    "sl:price-seed": "node scripts/sl-build/extract-price-seed.js"
  },
  "author": "Sarcastic Software Studios",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/sarcasticsoftwarestudio/mana-ledger.git"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.0",
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "electron": "^42.6.1",
    "electron-builder": "^25.0.0",
    "svelte": "^4.2.19",
    "vite": "^5.4.0",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "chart.js": "^4.5.1",
    "electron-updater": "^6.8.3",
    "interactjs": "^1.10.27"
  },
  "build": {
    "appId": "com.akapl.secretlairtracker",
    "productName": "Mana Ledger",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "package.json"
    ],
    "win": {
      "icon": "build/icon.png",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "perMachine": false
    },
    "publish": [
      {
        "provider": "github",
        "owner": "sarcasticsoftwarestudio",
        "repo": "mana-ledger",
        "releaseType": "release"
      }
    ]
  }
}
